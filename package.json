{
    "name": "pass-selling-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "cd backend && npm install",
        "start": "node backend/src/server.js",
        "build": "cd backend && npm install"
    }
}